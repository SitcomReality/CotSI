## Refactor Plan

The `src/render/hexmap3d/` directory had 28 files in a single flat folder, most of which have been moved into subdirectories. Some of the filenames have also changed.

Our current goal is to update imports, exports, references and links.

