/**
 * combatUiParams.js — Combat modal animation timings, icon sizes, and UI constants.
 */

/** Bot pick delay before auto-choosing (ms). */
export const BOT_PICK_DELAY_MS = 450;
/** Extra hold time after reveal animation (ms). */
export const REVEAL_HOLD_MS = 1200;
/** Clash pulse class removal delay (ms). */
export const CLASH_PULSE_MS = 800;
/** Default count-up duration (ms). */
export const COUNT_UP_DURATION_MS = 500;
/** Float text fallback cleanup timeout (ms). */
export const FLOAT_TEXT_CLEANUP_MS = 1000;
/** HP drain transition assumed duration (ms). */
export const HP_DRAIN_TRANSITION_MS = 420;
/** Slot flip wait time (ms). */
export const SLOT_FLIP_WAIT_MS = 420;
/** Clash pause before count-up starts (ms). */
export const CLASH_PAUSE_MS = 150;
/** Wait after damage animation before next round (ms). */
export const ROUND_END_HOLD_MS = 1200;

/** Slot reveal icon size (px). */
export const REVEAL_ICON_SIZE = 20;
/** Potency pip glyph icon size (px). */
export const PIP_GLYPH_SIZE = 14;
