/**
 * setupParams.js — Setup screen default values and slider ranges.
 */

/** Default map radius fallback (when template value unavailable). */
export const DEFAULT_MAP_RADIUS = 21;
/** Default relic target fallback. */
export const DEFAULT_RELIC_TARGET = 25;

/** Default height variation multiplier. */
export const DEFAULT_HV = 1.0;
/** Default wateriness multiplier. */
export const DEFAULT_WT = 1.0;
/** Default mountainousness multiplier. */
export const DEFAULT_MT = 1.0;

/** Seed random multiplier (Math.random() * N). */
export const SEED_RANDOM_MAX = 9999;
